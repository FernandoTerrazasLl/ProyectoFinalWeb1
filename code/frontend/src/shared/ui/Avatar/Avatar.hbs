<div class="avatar avatar--{{size}}">
  {{#if src}}
    <img class="avatar__image" src="{{src}}" alt="{{alt}}" width="{{pixelSize}}" height="{{pixelSize}}" loading="lazy">
  {{else}}
    <span class="avatar__initials" aria-label="{{alt}}">{{initials}}</span>
  {{/if}}
</div>
